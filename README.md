In this the first task was completed successfully I've collected the cloud watch logs and its in the snippets I also use cloud trail and in the second we deploys a web application on two Amazon EC2 instances behind an Application Load Balancer (ALB). 
The ALB distributes incoming HTTP requests across both instances, providing high availability and load balancing. 
Users access the application through the ALB DNS name, and refreshing the URL demonstrates traffic being routed between the two EC2 instances.
